package za.ac.cput.cputrade.product;

/** Listing category — drives the category filter pills (US3.1). */
public enum Category {
    TEXTBOOKS,
    ELECTRONICS,
    CLOTHING,
    SERVICES,
    OTHER
}
