package za.ac.cput.cputrade.bulletin;

/**
 * What kind of notice a bulletin post is. LOST/FOUND turn the board's
 * general-purpose "anything goes" text box into a structured, filterable
 * Lost & Found section — genuinely useful (and shareable) even before the
 * marketplace itself has much listed, since it needs no seller at all.
 */
public enum BulletinType {
    GENERAL,
    LOST,
    FOUND
}
